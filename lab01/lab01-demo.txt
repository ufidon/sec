# 1. Transfer files between VMs
	# On the Ubuntu VM, download two images, one simple and one complex
	
	# On the Windows VM, create a text file containing secret message
	
	# run ftp client from the Windows VM to connect the ftp server on the Ubuntu VM
	
	# use ftp to download the images on the Ubuntu VM
	
	# use the Paint program to open the two images and save them in 24bit BMP format
	
	# through ftp, upload the two 24bit BMP files and the secret text file onto the Ubuntu VM

# 2. Encrypt & decrypt images, hide secret message in images
	# use git to pull files from https://github.com/ufidon/its250.git

	# use the script 'encdecpic.sh' to encrypt then decrypt images
	
	# use cat to hide secret message in images
	
	# use the script 'stripsecret.sh' to get the secret messages from images with secrets